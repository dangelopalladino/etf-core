/** @type {import('next').NextConfig} */
const nextConfig = {
  // Compile etf-core's TS sources from the parent dir directly.
  transpilePackages: [],
  experimental: {},
};

export default nextConfig;
